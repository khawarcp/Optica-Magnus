<div class="news-card">
  <a href="news-details.html">
    <div class="img-block">
      <img src="{{ article.image | img_url: 'master' }}" loading="lazy" alt="{{ article.title }}" class="img-fluid">
    </div>
  </a>
  <div class="desc-block">
    {%- if show_date -%}
      <span class="date" {%- if show_author -%} style="margin-right:10px;"{%- endif -%}>
        {{ article.published_at | time_tag: format: 'date' }}
      </span>
    {%- endif -%}
    {%- if show_author -%}
      <span>{{ article.author }}</span>
    {%- endif -%}
    <h5 class="news-title">
      {{ article.title | truncate: 50 | escape }}
    </h5>
    {%- if show_excerpt -%}
      {%- if article.excerpt.size > 0 or article.content.size > 0 -%}
        <p class="article-card__excerpt rte-width">
          {%- if article.excerpt.size > 0 -%}
            {{ article.excerpt | strip_html | truncatewords: 10 }}
          {%- else -%}
            {{ article.content | strip_html | truncatewords: 10 }}
          {%- endif -%}
        </p>
      {%- endif -%}
    {%- endif -%}
    <a class="readMore-btn" href="news-details.html">
      Read More {% render 'icon-read-more' %}
    </a>
  </div>
</div>